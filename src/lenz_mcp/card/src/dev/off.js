// The production build resolves `lenz-card-dev` here: no fixture switcher, no
// fixtures. build.mjs swaps in switcher.jsx only for the dev probe's bundle.
export const createDevHost = null;
export const DevSwitcher = null;
